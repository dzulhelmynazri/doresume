const ApplicationFormPage = () => (
  <p>
    Form: this will be different form based on ATS (Greenhouse, Lever, Taleo,
    etc). For entire /dashboard/applications/[id] pages, is what we show user
    what our Agent submitted.
  </p>
);

export default ApplicationFormPage;
